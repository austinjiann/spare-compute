CONNECTIVITY_DOMAIN=connect.example.com
ACME_EMAIL=admin@example.com
TURN_REALM=turn.example.com
VPS_PUBLIC_IP=203.0.113.10
# Usually the same as VPS_PUBLIC_IP. If the provider uses 1:1 NAT, use the
# host's primary private IPv4 here instead.
TURN_RELAY_IP=203.0.113.10
COMPUTEHOP_VERSION=dev
