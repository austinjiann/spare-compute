# Hosted deployment

This directory will contain environment-specific configuration for the opaque
connectivity service, coturn, and monitoring. It must never contain production
credentials or device-private state.
